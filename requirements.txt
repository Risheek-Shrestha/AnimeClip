# Core
Django>=5.2,<5.3   # 5.2 LTS — supported until April 2028
python-dotenv>=1.0.0

# Database
psycopg2>=2.9.9    # use the source package in production (not -binary)

# Media / Static
cloudinary>=1.36.0
django-cloudinary-storage>=0.3.0
whitenoise>=6.6.0
Pillow>=10.0.0

# Cache
django-redis>=5.4.0
django-ratelimit>=4.1.0

# Production server
gunicorn>=21.2.0

# Recommendations
numpy>=1.26.0
scikit-learn>=1.4.0

# Error monitoring
sentry-sdk[django]>=2.0.0

# Async task queue + periodic scheduler
celery>=5.3.0
celery[redis]>=5.3.0
django-celery-beat>=2.6.0

# Brute-force login protection
django-axes>=6.4.0

# Content-Security-Policy headers
django-csp>=4.0

# Code quality
ruff>=0.4.0

# Watch Party (future WebSocket upgrade) — install to enable real-time sync
# channels>=4.1.0
# channels-redis>=4.2.0
# daphne>=4.1.0
