# Core
Django>=5.2,<5.3   # 5.2 LTS — supported until April 2028
python-dotenv>=1.0.0

# Database
psycopg2>=2.9.9    # use the source package in production (not -binary)

# Media / Static
cloudinary>=1.36.0
django-cloudinary-storage>=0.3.0
whitenoise>=6.6.0
Pillow>=10.0.0

# Cache
django-redis>=5.4.0
django-ratelimit>=4.1.0

# Production server
gunicorn>=21.2.0

# Recommendations
numpy>=1.26.0
scikit-learn>=1.4.0

# Error monitoring
sentry-sdk[django]>=2.0.0

# Async task queue + periodic scheduler
celery[redis]>=5.3.0
django-celery-beat>=2.6.0

# Brute-force login protection
django-axes>=6.4.0

# Content-Security-Policy headers
django-csp>=4.0

# Code quality
ruff>=0.4.0

# Feature Pack additions
pyotp>=2.9.0                # 2FA TOTP generation and verification
qrcode[pil]>=7.4.2         # QR code generation for 2FA setup
pywebpush>=2.0.0           # Web Push API (VAPID) — no FCM/APNs needed
geoip2>=4.7.0              # MaxMind GeoLite2 database reader (geo-blocking)
channels>=4.1.0            # Django Channels WebSockets (live support chat / watch party)
channels-redis>=4.2.0      # Redis channel layer for Channels
daphne>=4.1.0              # ASGI server for Channels (replaces/supplements gunicorn)

# Billing
stripe>=10.0.0              # Stripe Checkout + webhook verification for subscription.py
